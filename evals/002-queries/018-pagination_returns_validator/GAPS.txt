queries, pagination_returns_validator:

- Beyond regular pagination struggles, the returns validator doesn't include the `splitCursor` or `pageStatus` fields.
- covered by guideline: use `paginationResultValidator(itemValidator)` and the paginated return fields `page`, `isDone`, `continueCursor`.
