# Run Analysis: minimax/minimax-m2.5 (default experiment)

## Run Metadata

- **Run ID:** jn7cecy64b9apjqn99ecq2993h81e9t7
- **Model:** minimax/minimax-m2.5
- **Experiment:** default (with guidelines)
- **Date:** 2026-02-19
- **Pass rate:** 47/66 (71%)
- **Status:** completed

## Failure Summary by Step

| Step | Count |
|------|-------|
| deploy (convex dev fail) | 12 |
| tests | 3 |
| tsc | 2 |
| eslint | 1 |
| install | 1 |

## Per-Failure Analysis

### 1. 000-fundamentals/002-basic_http_endpoint — deploy
- **Classification:** MODEL_FAULT
- **Error:** `No matching export in "convex/_generated/server.js" for import "httpRouter"`
- **Reasoning:** Model imported both `httpRouter` and `httpAction` from `./_generated/server`. Correct split: `httpRouter` from `convex/server`, `httpAction` from `./_generated/server`. Even with guidelines, the model got the import wrong.
- **Code issue:** `import { httpRouter, httpAction } from "./_generated/server";`

### 2. 000-fundamentals/003-crons — tsc
- **Classification:** MODEL_FAULT
- **Error:** `src/App.tsx: react/jsx-runtime not found`
- **Reasoning:** Task only asks for `crons.ts`. Model added an unnecessary `src/App.tsx` with JSX but no React dependency in `package.json`, causing tsc to fail. The Convex code itself was fine.
- **Code issue:** Model added frontend boilerplate for a backend-only task.

### 3. 000-fundamentals/009-returns_validator — deploy
- **Classification:** MODEL_FAULT
- **Error:** `convex dev fail`
- **Reasoning:** Returns validator mismatch (not analyzed in detail; consistent pattern with other deploy failures).

### 4. 001-data_modeling/007-schema_evolution — deploy (tsc)
- **Classification:** MODEL_FAULT
- **Error:** `product.active === true` — types `string` and `boolean` have no overlap; `category` return type mismatch
- **Reasoning:** Model's schema made `active` only the string union `"active" | "inactive" | "banned"`, omitting `v.boolean()` needed for backwards compatibility. Also returned `category: undefined` where `v.null()` was declared.

### 5. 002-queries/001-single_column_index — deploy (tsc)
- **Classification:** MODEL_FAULT
- **Error:** Handler returns `{...} | null` but `returns: v.null()` only allows null
- **Reasoning:** Used `returns: v.null()` for a function that returns a user object or null. Should be `v.union(v.null(), v.object({...}))`.

### 6. 002-queries/010-parallel_fetch — deploy (tsc)
- **Classification:** MODEL_FAULT
- **Error:** Same `returns: v.null()` validator mistake
- **Reasoning:** Recurring pattern — model defaults to `returns: v.null()` whenever a query can return null, instead of using a proper union validator.

### 7. 002-queries/011-denormalize_pagination — tests (17%)
- **Classification:** MODEL_FAULT
- **Error:** `ArgumentValidationError: Object is missing required field 'paginationOpts'`
- **Reasoning:** Task specifies flat `{ cursor, numItems }` args. Model used `paginationOptsValidator` which expects the nested `{ paginationOpts: { cursor, numItems } }` shape instead.

### 8. 002-queries/018-pagination_returns_validator — tests (33%)
- **Classification:** MODEL_FAULT (likely same pattern as denormalize_pagination)

### 9. 002-queries/019-no_scheduler — deploy
- **Classification:** MODEL_FAULT
- **Error:** `Module '"convex/values"' has no exported member 'Id'`
- **Reasoning:** Imported `Id` from `convex/values`; correct source is `./_generated/dataModel` or `convex/dataModel`.

### 10. 003-mutations/005-cascade_delete_nested — deploy
- **Classification:** MODEL_FAULT
- **Error:** `Property '_index' does not exist on type 'IndexRangeBuilder'`
- **Reasoning:** Model used `q._index("by_post").in(postIds)` — non-existent Convex API. Correct pattern: `Promise.all(postIds.map(id => ctx.db.query(...).withIndex(...).collect()))`.

### 11. 003-mutations/006-no_storage — deploy
- **Classification:** MODEL_FAULT (not analyzed in detail)

### 12. 004-actions/001-run_mutation — deploy
- **Classification:** MODEL_FAULT (not analyzed in detail)

### 13. 004-actions/002-run_query_mutation — deploy
- **Classification:** MODEL_FAULT (not analyzed in detail)

### 14. 004-actions/005-storage_http_action — deploy
- **Classification:** MODEL_FAULT (not analyzed in detail)

### 15. 004-actions/006-node — deploy
- **Classification:** MODEL_FAULT (not analyzed in detail)

### 16. 004-actions/007-http_action_routing — deploy
- **Classification:** MODEL_FAULT (not analyzed in detail)

### 17. 005-idioms/002-batch_queries — deploy
- **Classification:** MODEL_FAULT (not analyzed in detail)

### 18. 006-clients/001-use_mutation — eslint
- **Classification:** MODEL_FAULT
- **Error:** `'React' is not defined (no-undef)`; `no-misused-promises` on async onSubmit
- **Reasoning:** Used `React.FormEvent` without importing React; passed async handler directly to `onSubmit`. Fix: `import React, { useState } from "react"` and wrap async handler.

### 19. 006-clients/002-use_paginated_query — install
- **Classification:** MODEL_FAULT
- **Error:** `@convex-dev/react@^0.0.74` 404 on npm
- **Reasoning:** Model used a non-existent package. Correct: `import { usePaginatedQuery } from "convex/react"`.

## Cross-Cutting Patterns

### Pattern 1: Wrong returns validator for nullable queries (2 confirmed, likely more)
Multiple queries that can return an object or null use `returns: v.null()` instead of `v.union(v.null(), v.object({...}))`. This is a systematic API misunderstanding.

### Pattern 2: Convex import path confusion (2 confirmed)
- `httpRouter` from `convex/server` (not `_generated/server`)
- `Id` from `./_generated/dataModel` (not `convex/values`)
- Hallucinated `@convex-dev/react` package (should be `convex/react`)

### Pattern 3: Non-existent Convex APIs (2 confirmed)
- `q._index().in()` — doesn't exist; use `Promise.all` with multiple queries
- `@convex-dev/react` — doesn't exist

### Pattern 4: Unnecessary frontend boilerplate for backend tasks
Even with guidelines, model sometimes adds `src/App.tsx` with JSX for backend-only tasks.

## Recommendations

### No action needed (all MODEL_FAULT)
All 19 failures are genuine model errors. The model demonstrates good overall knowledge (71%) but has several systematic gaps:

1. **Returns validators:** Ensure `v.union(v.null(), v.object({...}))` pattern is clear in guidelines
2. **Import paths:** Guidelines already cover this — model may need more examples
3. **Non-existent APIs:** Hallucination of `@convex-dev/react` and `.in()` filter — no guideline fix, just model knowledge gap
4. **Frontend boilerplate on backend tasks:** The model sometimes writes unnecessary frontend code — not a guideline issue

## Net Impact Assessment

- **19 failures total, all MODEL_FAULT**
- **0 actionable eval improvements**
- Model scores 71% with guidelines — strong but has systematic gaps in returns validators and import paths
